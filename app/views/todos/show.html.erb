<div>
  <%= render 'todo', todo: @todo %>
</div>

<ul class="comments">
  <% if @comments.each do |comment| %>
  <li><%= render 'comment', comment: comment %></li>
  <% end.empty? %>
  <p> 还没有评论... </p>
  <% end %>
</ul>
